# Add this value in Vercel Project Settings > Environment Variables.
# Keep the real browser key out of Git and restrict it in Google Cloud.
GOOGLE_MAPS_KEY=replace_with_a_restricted_browser_key

# Public Google Cloud map ID used by AdvancedMarkerElement.
GOOGLE_MAPS_MAP_ID=replace_with_your_map_id
